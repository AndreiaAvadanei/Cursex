import numpy as np

# Create a vector as a row
vector_row = np.array([1, 2, 3])

# Ctreate a vector as a column
vector_column = np.array([[1, 4],
                         [2, 5],
                         [3, 6]]) # this is how we create a matrics
v = np.eye(10)
#matricea in oglinda

print(vector_row)
print(vector_column)
print(v)